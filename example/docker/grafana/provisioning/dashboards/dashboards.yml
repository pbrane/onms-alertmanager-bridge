apiVersion: 1

providers:
  - name: 'OpenNMS Dashboards'
    orgId: 1
    folder: 'OpenNMS'
    folderUid: 'opennms'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
